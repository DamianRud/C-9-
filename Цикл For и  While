using System;

namespace C__за_9_часов
{
    internal class Class1
    {
        static void Main(string[] args)
        {

            {
                //for (int i = 0; i < 10; i++)//int i=0 начало цикла, i<10 условие продолжения цикла, i++ увеличение счетчика на единицу после каждой итерации
                {
                    // Console.WriteLine($"Привет, это сообщение номер {i + 1}");//выводим на экран приветствие и номер сообщения, i+1 потому что счетчик начинается с 0, а нам нужно начать нумерацию с 1
                }
                {
                    //for (int i = 0; i < 10; i += 2)//int i=0 начало цикла, i<10 условие продолжения цикла, i+=2 увеличение счетчика на 2 после каждой итерации
                    //Console.WriteLine(i);//выводим на экран значение счетчика, которое будет увеличиваться на 2 после каждой итерации, начиная с 0

                    {
                        for (int i = 10; i > 0; i--)//int i=10 начало цикла, i>0 условие продолжения цикла, i-- уменьшение счетчика на единицу после каждой итерации
                            Console.WriteLine(i);

                    }
                }
            }
        }
    }
}





















using System;

namespace C__за_9_часов
{
    internal class Class1
    {
        static void Main(string[] args)
        {

            {
                //for (int i = 0; i < 10; i++)//int i=0 начало цикла, i<10 условие продолжения цикла, i++ увеличение счетчика на единицу после каждой итерации
                {
                    // Console.WriteLine($"Привет, это сообщение номер {i + 1}");//выводим на экран приветствие и номер сообщения, i+1 потому что счетчик начинается с 0, а нам нужно начать нумерацию с 1
                }
                {
                    //for (int i = 0; i < 10; i += 2)//int i=0 начало цикла, i<10 условие продолжения цикла, i+=2 увеличение счетчика на 2 после каждой итерации
                    //Console.WriteLine(i);//выводим на экран значение счетчика, которое будет увеличиваться на 2 после каждой итерации, начиная с 0

                    {
                        //for (int i = 10; i > 0; i--)//int i=10 начало цикла, i>0 условие продолжения цикла, i-- уменьшение счетчика на единицу после каждой итерации
                            //Console.WriteLine(i);

                    }
                    {
                        int age;
                        Console.WriteLine("Введите возраст");

                        age = Convert.ToInt32(Console.ReadLine());//считываем возраст с консоли и преобразуем его в целое число

                        for (int i = 0; i <age; i++)//int i=0 начало цикла, i<age условие продолжения цикла, i++ увеличение счетчика на единицу после каждой итерации
                        {
                            Console.WriteLine("Вам исполнилось"+i);
                        }

                    }
                }
            }
        }
    }
}




















using System;

namespace C__за_9_часов
{
    internal class Class1
    {
        static void Main(string[] args)
        {

            {
                //for (int i = 0; i < 10; i++)//int i=0 начало цикла, i<10 условие продолжения цикла, i++ увеличение счетчика на единицу после каждой итерации
                {
                    // Console.WriteLine($"Привет, это сообщение номер {i + 1}");//выводим на экран приветствие и номер сообщения, i+1 потому что счетчик начинается с 0, а нам нужно начать нумерацию с 1
                }
                {
                    //for (int i = 0; i < 10; i += 2)//int i=0 начало цикла, i<10 условие продолжения цикла, i+=2 увеличение счетчика на 2 после каждой итерации
                    //Console.WriteLine(i);//выводим на экран значение счетчика, которое будет увеличиваться на 2 после каждой итерации, начиная с 0

                    {
                        //for (int i = 10; i > 0; i--)//int i=10 начало цикла, i>0 условие продолжения цикла, i-- уменьшение счетчика на единицу после каждой итерации
                        //Console.WriteLine(i);

                    }
                    {
                        int age;
                        Console.WriteLine("Введите возраст");

                        age = Convert.ToInt32(Console.ReadLine());//считываем возраст с консоли и преобразуем его в целое число

                        for (int i = 0; i < 100; i+=2)// int i=0 начало цикла, i<100 условие продолжения цикла, i+=2 увеличение счетчика на 2 после каждой итерации
                        {
                            Console.WriteLine("Вам исполнилось" + i);//выводим на экран сообщение с возрастом, который будет увеличиваться на 2 после каждой итерации, начиная с 0 и заканчивая возрастом, который ввел пользователь
                        }

                    }
                }
            }
        }
    }
}












using System;

namespace C__за_9_часов
{
    internal class Class1
    {
        static void Main(string[] args)
        {
            {
                int tirescount = 5;
                string password = "12345";
                string user;

                for (int i = 0; i < tirescount; i++)
                {

                    Console.WriteLine("Введите пароль");
                    user = Console.ReadLine();

                    if (user == password)
                    {
                        Console.WriteLine("Пароль верный");
                        break;
                    }
                    else
                    {
                        Console.WriteLine("Пароль неверный");
                        Console.WriteLine("У вас осталось попыток " + (tirescount - i - 1));//вывод количества оставшихся попыток
                    }
                }
            }
        }
    }
}
























using System;

namespace C__за_9_часов
{
    internal class Class1
    {
        static void Main(string[] args)
        {
            {
                float money;
                int years;
                int percent;

                Console.Write("Введите количество денег, внесенных на вклад: ");
                money = Convert.ToSingle(Console.ReadLine());

                Console.Write("На сколько лет открыт вклад? ");
                years = Convert.ToInt32(Console.ReadLine());

                Console.Write("Под какой процент? ");
                percent = Convert.ToInt32(Console.ReadLine());

                for (int i = 0; i < years; i++)
                {
                    money += money / 100 * percent;
                    Console.WriteLine("В этом году у вас " + money);
                }

                Console.ReadKey();

            }
                }
            }
        }
















using System;

namespace C__за_9_часов
{
    internal class Class1
    {
        static void Main(string[] args)
        {
            {
                int playerHealth = 1000;
                int playerDamage = 5;
                int enemyHealth = 1500;
                int enemyDamage = 3;

                while (playerHealth > 0 && enemyHealth > 0)
                {
                    playerHealth -= enemyDamage;
                    enemyHealth -= playerDamage;

                    Console.WriteLine(playerHealth + " игрок.");
                    Console.WriteLine(enemyHealth + " враг.");
                }

                if (playerHealth <= 0 && enemyHealth <= 0)
                {
                    Console.WriteLine("Ничья.");
                }
                else if (enemyHealth <= 0)
                {
                    Console.WriteLine("Победа игрока");
                }
                else if (playerHealth <= 0)
                {
                    Console.WriteLine("Победа врага.");
                }

            }
        }
    }
}










